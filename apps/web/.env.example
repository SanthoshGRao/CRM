NEXT_PUBLIC_API_URL=http://147.93.108.218:4001
PORT=3002

# Android push notifications. Leave unset/false until the Android app has a
# real google-services.json built in AND the API has FIREBASE_SERVICE_ACCOUNT
# set — see apps/mobile/README.md. Turning this on without both in place
# crashes the Android app on login.
NEXT_PUBLIC_PUSH_ENABLED=false
